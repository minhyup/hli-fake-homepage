import React, { FC } from "react";
import { Button } from "antd";
import "./App.less";

const App: FC = () => (
  <div>
    <Button type="primary">Button</Button>
    <Button>버튼2</Button>
  </div>
);

export default App;
